This micro-plugin for the [Vim](https://www.vim.org/) editor changes the cursor
color depending on current mode.

Example configuration:

	let g:cursor_color = {
		\"normal": "green",
		\"insert": "white",
		\"replace": "orange",
	\}
